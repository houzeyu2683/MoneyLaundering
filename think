
測試集散佈在訓練集的資料庫中，首先需要對整個資料集的每筆資料做一個標籤，註明是訓練集還是測試集。

提交的格式是表格，其中有兩個欄位，分別是`alert_key`與`probability`欄位，從這邊就可以知道後續在做特徵工程的時候，需要依照`alert_key`來建構，這是一個很重要的資訊。

給自己一個限制，本次比賽不使用深度學習框架，使用其他機器學習的技法來參加這場比賽。

逐一對每個資料表進行前處理，前處理的定義是對資料進行以下處理，屬於非特徵工程流程。

- 數值轉換：針對類別變數進行數值化，方便後續操作。
- 值域約束：針對異常值處理。
- 填補缺失：針對遺失值進行填補。

特徵工程



